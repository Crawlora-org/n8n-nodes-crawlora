node_modules/
dist/
*.log
.DS_Store
.env

# Intermediate OpenAPI v3 spec (regenerated by `npm run gen`); the committed
# properties.json is the artifact the node actually uses.
nodes/Crawlora/openapi.json

# Local redaction rules (not published)
scripts/redactions.local.json
